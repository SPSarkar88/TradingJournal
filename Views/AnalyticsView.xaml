<UserControl x:Class="TradingJournal.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource HeroCardStyle}">
            <StackPanel>
                <TextBlock Text="{Binding Title}"
                           Style="{StaticResource PageTitleTextStyle}" />
                <TextBlock Text="{Binding Subtitle}"
                           Style="{StaticResource PageSubtitleTextStyle}" />
                <TextBlock Text="{Binding Overview}"
                           Margin="0,16,0,0"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource SecondaryTextBrush}" />
            </StackPanel>
        </Border>

        <UniformGrid Grid.Row="1"
                     Margin="0,24,0,0"
                     Columns="3">
            <Border Style="{StaticResource MetricCardStyle}" Margin="0,0,18,0">
                <StackPanel>
                    <TextBlock Text="Win Rate" Style="{StaticResource CardLabelTextStyle}" />
                    <TextBlock Text="62.4%" Style="{StaticResource MetricValueTextStyle}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource MetricCardStyle}" Margin="0,0,18,0">
                <StackPanel>
                    <TextBlock Text="Net P and L" Style="{StaticResource CardLabelTextStyle}" />
                    <TextBlock Text="+18,740" Style="{StaticResource MetricValueTextStyle}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource MetricCardStyle}">
                <StackPanel>
                    <TextBlock Text="Max Drawdown" Style="{StaticResource CardLabelTextStyle}" />
                    <TextBlock Text="-4.9%" Style="{StaticResource MetricValueTextStyle}" />
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>
