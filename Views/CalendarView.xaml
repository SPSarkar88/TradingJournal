<UserControl x:Class="TradingJournal.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,18">
            <Button Content="Previous" Command="{Binding PreviousMonthCommand}" Style="{StaticResource GhostButtonStyle}" />
            <TextBlock Text="{Binding DisplayMonthLabel}" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="16,0" />
            <Button Content="Next" Command="{Binding NextMonthCommand}" Style="{StaticResource GhostButtonStyle}" />
            <TextBlock Text="{Binding ActiveAccountDisplay}"
                       Margin="20,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource SecondaryTextBrush}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="0.8*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource PanelCardStyle}">
                <ItemsControl ItemsSource="{Binding Days}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="7" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.SelectDayCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Margin="4"
                                    Padding="10"
                                    Background="{Binding HeatLevel, Converter={StaticResource HeatBrushConverter}}"
                                    BorderBrush="{StaticResource BorderBrushSoft}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Date.Day}" FontWeight="Bold" />
                                    <TextBlock Text="{Binding TotalPnL, StringFormat=N0}" FontSize="12" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource PanelCardStyle}">
                <StackPanel>
                    <TextBlock Text="Selected Day Trades" Style="{StaticResource CardTitleTextStyle}" />
                    <ItemsControl Margin="0,16,0,0" ItemsSource="{Binding SelectedDayTrades}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="12" Padding="12" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Symbol}" FontWeight="SemiBold" />
                                        <TextBlock Grid.Column="1" Text="{Binding NetPnL, StringFormat=N2}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
