<UserControl x:Class="TradingJournal.Views.StrategyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.1*" />
            <ColumnDefinition Width="18" />
            <ColumnDefinition Width="0.9*" />
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource PanelCardStyle}">
            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleTextStyle}" />
                <TextBlock Text="{Binding ActiveAccountDisplay}" Margin="0,10,0,0" Foreground="{StaticResource SecondaryTextBrush}" />
                <TextBlock Text="{Binding StatusMessage}" Margin="0,10,0,18" Foreground="{StaticResource SecondaryTextBrush}" />
                <DataGrid AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          ItemsSource="{Binding Strategies}"
                          SelectedItem="{Binding SelectedStrategy}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Trades" Binding="{Binding TradeCount}" />
                        <DataGridTextColumn Header="P and L" Binding="{Binding TotalPnL, StringFormat=N2}" />
                        <DataGridTextColumn Header="Win Rate" Binding="{Binding WinRate, StringFormat=N1}" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource PanelCardStyle}">
            <StackPanel>
                <TextBlock Text="Strategy Editor" Style="{StaticResource CardTitleTextStyle}" />
                <TextBlock Text="Name" Style="{StaticResource CardLabelTextStyle}" Margin="0,16,0,0" />
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Description" Style="{StaticResource CardLabelTextStyle}" Margin="0,16,0,0" />
                <TextBox Height="90"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />
                <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                    <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                    <Button Content="New" Command="{Binding NewCommand}" Style="{StaticResource GhostButtonStyle}" Margin="12,0,0,0" />
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource GhostButtonStyle}" Margin="12,0,0,0" />
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource GhostButtonStyle}" Margin="12,0,0,0" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
