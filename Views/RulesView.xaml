<UserControl x:Class="TradingJournal.Views.RulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.1*" />
            <ColumnDefinition Width="18" />
            <ColumnDefinition Width="0.9*" />
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource PanelCardStyle}">
            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleTextStyle}" />
                <TextBlock Text="{Binding StatusMessage}" Margin="0,10,0,18" Foreground="{StaticResource SecondaryTextBrush}" />
                <DataGrid AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          ItemsSource="{Binding Rules}"
                          SelectedItem="{Binding SelectedRule}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Type" Binding="{Binding RuleType}" />
                        <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" />
                        <DataGridTextColumn Header="Value" Binding="{Binding IntValue}" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource PanelCardStyle}">
            <StackPanel>
                <TextBlock Text="Rule Editor" Style="{StaticResource CardTitleTextStyle}" />
                <TextBlock Text="Name" Style="{StaticResource CardLabelTextStyle}" Margin="0,14,0,0" />
                <TextBox Text="{Binding Name}" />
                <TextBlock Text="Type" Style="{StaticResource CardLabelTextStyle}" Margin="0,14,0,0" />
                <ComboBox ItemsSource="{Binding RuleTypes}" SelectedItem="{Binding SelectedRuleType}" />
                <TextBlock Text="Int Value" Style="{StaticResource CardLabelTextStyle}" Margin="0,14,0,0" />
                <TextBox Text="{Binding IntValueText}" />
                <CheckBox Content="Boolean Value"
                          IsChecked="{Binding BoolValue}"
                          Margin="0,14,0,0" />
                <CheckBox Content="Active"
                          IsChecked="{Binding IsActive}"
                          Margin="0,8,0,0" />
                <TextBlock Text="Description" Style="{StaticResource CardLabelTextStyle}" Margin="0,14,0,0" />
                <TextBox Height="90"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Text="{Binding Description}" />
                <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                    <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                    <Button Content="New" Command="{Binding NewCommand}" Style="{StaticResource GhostButtonStyle}" Margin="12,0,0,0" />
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource GhostButtonStyle}" Margin="12,0,0,0" />
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource GhostButtonStyle}" Margin="12,0,0,0" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
