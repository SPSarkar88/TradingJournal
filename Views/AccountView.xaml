<UserControl x:Class="TradingJournal.Views.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource HeroCardStyle}">
            <StackPanel>
                <TextBlock Text="{Binding Title}"
                           Style="{StaticResource PageTitleTextStyle}" />
                <TextBlock Text="{Binding ActiveAccountDisplay}"
                           Margin="0,10,0,0"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           TextWrapping="Wrap" />
                <TextBlock Text="{Binding StatusMessage}"
                           Margin="0,10,0,0"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource PanelCardStyle}" Margin="0,18,0,18">
            <WrapPanel>
                <Button Content="New Account"
                        Command="{Binding NewCommand}"
                        Style="{StaticResource PrimaryButtonStyle}" />
                <Button Content="Edit Selected"
                        Command="{Binding EditCommand}"
                        Style="{StaticResource GhostButtonStyle}"
                        Margin="12,0,0,0" />
                <Button Content="Set Active"
                        Command="{Binding SetActiveCommand}"
                        Style="{StaticResource GhostButtonStyle}"
                        Margin="12,0,0,0" />
                <Button Content="Delete"
                        Command="{Binding DeleteCommand}"
                        Style="{StaticResource GhostButtonStyle}"
                        Margin="12,0,0,0" />
                <Button Content="Refresh"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource GhostButtonStyle}"
                        Margin="12,0,0,0" />
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource PanelCardStyle}">
            <DataGrid AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      ItemsSource="{Binding Accounts}"
                      SelectedItem="{Binding SelectedAccount}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Broker" Binding="{Binding Broker}" />
                    <DataGridTextColumn Header="Trades" Binding="{Binding TradeCount}" />
                    <DataGridTextColumn Header="Status" Binding="{Binding ActiveLabel}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
