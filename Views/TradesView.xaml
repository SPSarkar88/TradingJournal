<UserControl x:Class="TradingJournal.Views.TradesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:TradingJournal.Views"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.45*" />
            <ColumnDefinition Width="18" />
            <ColumnDefinition Width="0.95*" />
        </Grid.ColumnDefinitions>

        <views:TradeListingView DataContext="{Binding ListingViewModel}" />
        <views:TradeDetailView Grid.Column="2"
                               DataContext="{Binding DetailViewModel}" />
    </Grid>
</UserControl>
